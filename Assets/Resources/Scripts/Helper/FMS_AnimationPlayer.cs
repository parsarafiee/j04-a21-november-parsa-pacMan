using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using fsm;

public class FMS_AnimationPlayer : MonoBehaviour
{


    //State idle = new State("idle", rabit.IdleAnimation);
    //fsm.AddState(idle);
    //        fsm.SetInitialState(idle);

    //        State jump = new State("jump", rabit.Jump);
    //fsm.AddState(jump);





}
